# Nixpacks configuration for Coolify
# This ensures that FFmpeg and Python are installed

[phases.setup]
nixPkgs = ["...", "ffmpeg", "python3", "gcc"]

[phases.install]
cmds = ["npm install", "pip install -r requirements.txt --break-system-packages"]
